<% if(title) { %>
  <h1>
    <%= title %>
  </h1>
  <%} else { res.redirect('/'); } %>

  <%- messages() %>
  <% if (errors) { %>
    <ul class="notice">
      <% errors.array().forEach(error=> { %>
        <li>
          <%= error.msg %>
        </li>
        <% }) %>
    </ul>
  <% } %>


    <h2> Welcome <%- welcomeBasic  %> </h2>
    <div id="manageLinks">
      <h3>Update Account Info</h3>
      <p> <a href="/account/updateInfo/<%- acctId %>">Update Account Info</a></p>
      <% if(locals.accountData.account_type === "Admin" || 
            locals.accountData.account_type === "Employee") { %> 
          <h3>Inventory Management</h3>
          <p> <a href="/inv/">Inventory management</a></p>
      <% } %>
    </div>