<% if(title) { %>
  <h1 id="cart">
    <%= title %>
  </h1>
  <%} else { res.redirect('/'); } %>

  <%# userName %>

<%- messages() %>
<% if (errors) { %>
  <ul class="notice">
    <% errors.array().forEach(error=> { %><li>
                <%= error.msg %>
              </li>
              <% }) %>
          </ul>
          <% } %>